import {Parcel} from "../libs/parcel";
import * as React from 'react';
import * as ReactDOM from 'react-dom';

export default class GameContext extends Parcel<{},{}>{
  initialState(){
    return {}
  }

  listen(to){

  }
}